# Liftly (restored original files) — main branch

This repository contains the original Liftly frontend and server files. The upgrade/netlify-serverless branch contains Phase 0 Netlify serverless changes and new endpoints. Merge that branch to add Netlify Functions and user pages.
