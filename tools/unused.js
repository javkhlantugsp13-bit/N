const fs = require('fs');
const path = require('path');

const storePath = path.join(__dirname, '..', 'netlify', 'functions', '..', '..', 'data', 'store.json');
// note: this helper is unused; left as reference
